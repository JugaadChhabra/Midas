[pytest]
markers =
    live: read-only integration tests that hit the real Supabase project (deselect with -m "not live")
    needs_socket: legitimately opens a socket; exempt from the offline no-network guard (see tests/conftest.py)
