fastapi==0.115.5
uvicorn[standard]==0.32.1
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.151.0
supabase==2.10.0
python-dotenv==1.0.1
httpx==0.27.2
itsdangerous==2.2.0
json-repair==0.30.2
apscheduler==3.10.4
youtube-transcript-api>=0.6.2
# Pinned to an exact, verified-good version — NOT a floor. yt-dlp ships bad
# releases (2026.7.4 broke YouTube extraction on the box: "video not available"
# even with a valid PO token), so we never auto-upgrade to "newest". Bump this
# only after confirming a newer version actually downloads on the box, via PR.
# 2026.6.9 is the version running on the dev Mac that has never hit the error.
yt-dlp==2026.6.9
pytest==8.3.4
