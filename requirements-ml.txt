# Shorts cutter ML stack. Installed in Docker via the Dockerfile (torch/vision/audio
# come from the CPU wheel index first, then this file); also used by the local Mac venv.
# Pins mirror the working venv of the original RhymeShortsCutter_Mac app.
torch==2.12.1
torchvision==0.27.1
torchaudio==2.11.0
ultralytics==8.4.86
opencv-python==5.0.0.93
faster-whisper==1.2.1
ctranslate2==4.8.0
demucs==4.0.1
librosa==0.11.0
soundfile==0.14.0
av==18.0.0
numpy==2.4.6
# PO-token provider plugin (client half). MUST stay version-matched with the
# bgutil-provider server image in docker-compose.yml — a skew between the two
# makes the server mint tokens the plugin can't use, so yt-dlp silently falls
# back to a token-less client and YouTube lies "This video is not available".
bgutil-ytdlp-pot-provider==1.3.1
